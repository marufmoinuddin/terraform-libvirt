# ---------------------------------------------------------------------------
# cloudinit.tf — (empty) cloud-init logic is in the vm module
# ---------------------------------------------------------------------------
# Cloud-init user-data and network-config templates are rendered inside
# the vm module so each VM gets its own unique configuration.
